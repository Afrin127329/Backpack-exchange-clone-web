import LoginForm, { containerClassName } from "@/packages/components/Login";
const Login = () => {
  return (
    <div className={containerClassName}>
      <LoginForm />
    </div>
  );
};

export default Login;
